import {User} from "./user.model";
import {Subject} from "./subject.model";

export interface Classes {
  klassenID: number;
  klassenName: string;
  //user: number[];
  //subjects: number[];

  /*
  user: User[];
  subjects: Subject[];
   */

}
